dbt_project:
  target: dev

  outputs:
    dev:
      type: postgres
      host: host.docker.internal
      port: 5432
      user: postgres
      password: "{{ env_var('DBT_DB_PASSWORD') }}"
      dbname: sales_dw
      schema: warehouse
      threads: 4